pipeline {
  agent any
  stages {
    stage ('Build') {
      steps {
        ech 'Running build automation'
        sh './gradlew build --no-daemon'
        archiveArtifacts artifacts: 'dist/traingSchedule.zip'
      }
    }
  }
}
